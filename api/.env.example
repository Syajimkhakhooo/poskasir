# Database Configuration (Local PostgreSQL)
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=
DB_NAME=kasir_pos
DB_PORT=5432

# OR use connection string (Supabase)
# DATABASE_URL=postgresql://user:password@host:port/database

# Server Configuration
PORT=5000
NODE_ENV=development
